# ==================================
# LearnNest Backend - 生产环境配置
# 注意：敏感信息（密码、密钥）通过 .env.local 或环境变量注入
# ==================================

# 环境标识
RUST_ENV=production

# 日志级别（生产环境精简日志）
RUST_LOG=info,tower_http=info,sqlx=warn

# CORS（生产环境限定域名）
CORS_ALLOWED_ORIGINS=https://learnnest.example.com

# 数据库主机（密码通过 .env.local 注入）
DATABASE_HOST=db
DATABASE_PORT=5432
DATABASE_NAME=learnnest

# 生产环境关闭调试
RUST_BACKTRACE=0
